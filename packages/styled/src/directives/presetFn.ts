export const presetFn = (presetStr: string, styledRules: Record<string, any>) => {
  // console.log('presetFn.js |presetStr| = ', presetStr);
  // console.log('presetFn.js |styledRules| = ', styledRules);
};
