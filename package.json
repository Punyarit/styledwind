{
  "name": "styledwind",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compress": "gulp compress",
    "theme": "npx lerna run dev --scope=@styledwind/theme",
    "shared": "npx lerna run dev --scope=@styledwind/shared",
    "styled": "npx lerna run dev --scope=@styledwind/styled"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-uglify": "^3.0.2",
    "lerna": "^7.1.1"
  }
}
